'use strict';

describe('myApp.fiel module', function() {

  beforeEach(module('myApp.fiel'));

  describe('fiel controller', function(){

    it('should ....', inject(function($controller) {

      var fielCtrl = $controller('FielCtrl');
      expect(fielCtrl).toBeDefined();
    }));

  });
});
